# React + TypeScript + Vite + Tanstack Query + TailwindCSS + React Router

Ini adalah minimal setup (template) untuk react project.

## Usage

```
git clone git@github.com:feri-irawan/react-ts-starter.git
cd react-ts-starter
pnpm i
```
